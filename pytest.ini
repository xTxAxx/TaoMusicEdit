# pytest 配置：限定测试收集范围（detector / trimmer / webui/tests），
# 避免递归进入 webui/_vendor（vendored 依赖自带的测试会与系统安装的同名包冲突）。
[pytest]
testpaths =
    detector
    trimmer
    webui/tests
norecursedirs =
    .*
    build
    dist
    _vendor
    node_modules
    __pycache__
